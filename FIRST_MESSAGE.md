# First message to OpenCode

Use **Architect** mode and send:

按照 `AGENTS.md` 的规则，以 HyPanel 的 Sol Architect 身份开始工作。先读取 `docs/CONTEXT.md`、`docs/ROADMAP.md` 和 `TASKS.md`，检查当前仓库实际状态，从 `FE-000` 开始执行 Frontend Rescue。

所有需要工程判断、前端信息架构、组件边界、路由、API/数据流、交互设计、中大型实现、调试和最终集成都由你本人完成。没有 Terra agent。`luna` 只允许处理你已经给出明确参考实现后的机械重复工作，不得把完整功能或重构交给 Luna。

不要只输出计划，直接审计现有代码并持续修改、验证和 review，完成 `FE-010` 后再继续其它 Phase。除非必须改变产品方向、核心 Server-Agent Contract 或进行破坏性数据库迁移，否则不要停下来询问我。
